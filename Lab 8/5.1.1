#include <stdio.h>


int main(){

int num[100];
int chek[100];
int n  , i ,j ,x;
int ans ;

    printf("Input Array Size : ");
    scanf("%d",&n);
    

    
    for(i = 0 ; i < n ; i++){
        printf("Array [%d] :",i);
        scanf("%d",&num[i]);
    }
        j = i ;
         for(i = 0 ; i < n ; i++){
            printf("%d",num[i]);
             for(j = 0 ; j <= i ; j++){
                //printf("\t");
                printf("|%d ",num[j]);
                
                }
                    for(x = 0 ; x < i ; x++){
                    if(i != j){
                    printf("%d",ans = i != j); 
                    }
             }
               
         }
       
         
    return 0 ;
}